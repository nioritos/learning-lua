--palavra chave para uma variavel: local 
local variavel1 = "variavel1"

-- não podemos ter caracteres especiais (. ! ? . ; :  ) ou letras maiúsuculas começando em uma variavel, podemos fazer como no exemplo: 

local variavelLegal = "variavelLegal"

local variavel_legal = "variavel_legal"


-- Tipos de dados

-- tipos dados que uma variável aceita: string, number, null, booleans 

-- Tipos de dados - null


local dadosParaExibir = null -- tipo de variavél null, ou seja, você reserva um valor, porém não inputa qual tipo de dado que é. É a mesma coisa que fazer: 

local dadosParaExibir2

-- Tipos de dados - null*/

-- Tipos de dados - string

local stringDupla = "olá, isso aqui é uma string." -- tipo de varíavel string, ou seja, entre aspas duplas ("") ou simples (''), você pode guardar um texto/cadeia de caracteres.

local stringSimples = 'olá, isso aqui é uma string.'

-- Tipos de dados - string
-- Tipos de dados - number

local numeroInteiro = 123123123123123 /*tipo de dado number, ou seja, você pode guarda número inteiros/não decimais/sem vírgulas e/ou números demais/com vírgulas em um varíavel

local numeroDecimal = 1231.2312.3123.123
-- Tipos de dados - number


-- Tipos de dados - booleans

local valorVerdadeiro = true --tipo de dado boolean, mas especificamente: true; tipo valor que representa verdadeiro

local valorFalso = false --tipo de dado boolean, mas especificamente false, tipo de valor que representa falso

-- Tipos de dados - booleans


-- Tipos de dados

--Podemos fazer um tipo de coisa com variável chamada: reatribuição de valor de uma varíavel; ou seja, você substituíra o valor que uma varíavel já tem por um outro.

numeroDecimal = 123.321 /* aqui pegamos a varíavel chamada "numeroDecimal" e reatribuímos o valor dela para um outro, podemos fazer isso com qualquer varíavel 
